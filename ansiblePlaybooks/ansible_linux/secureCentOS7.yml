---
- name: Secure CentOS7
  hosts: all
  become: yes
  tasks:
    - name: Update yum repo
      yum:
        name: "*"
        state: latest

    - name: Stop SSH
      ansible.builtin.shell: "systemctl disable ssh.service && systemctl stop ssh.service"
      

